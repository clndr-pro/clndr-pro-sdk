{
  "name": "clndr-sdk-workspace",
  "private": true,
  "version": "0.1.1",
  "description": "Official SDK + React components for clndr.pro",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @clndr-pro/sdk -w @clndr-pro/react",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "license": "MIT"
}
